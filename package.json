{
  "name": "cors-forward",
  "version": "0.1.0",
  "description": "CORS Forward is a reverse proxy which adds CORS headers to the proxied request. Request URL is taken from the path",
  "license": "MIT",
  "author": "kirisaki77",
  "contributors": [
    "Rob Wu <rob@robwu.nl>"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kirisaki77/cors-forward.git"
  },
  "bugs": {
    "url": "https://github.com/kirisaki77/cors-forward/issues/"
  },
  "keywords": [
    "cors",
    "cross-domain",
    "http-proxy",
    "proxy",
    "heroku"
  ],
  "main": "./lib/cors-anywhere.js",
  "files": [
    "lib/",
    "README.ja.md",
    "server.js"
  ],
  "dependencies": {
    "httpxy": "0.5.5",
    "proxy-from-env": "2.1.0"
  },
  "devDependencies": {
    "@sinonjs/fake-timers": "^15.4.0",
    "c8": "^12.0.0",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "mocha": "^12.0.2",
    "supertest": "^7.2.2"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "mocha ./test/test*.js --reporter spec",
    "test-coverage": "c8 --reporter=text --reporter=lcov mocha test/test.js test/test-ratelimit.js test/test-security.js test/test-httpxy.js --reporter spec"
  },
  "engines": {
    "node": "^22.13.0 || >=24.0.0"
  }
}
